# OutreachPass Frontend Environment Variables
# Copy this file to .env.local and fill in your values

# API Configuration
NEXT_PUBLIC_API_URL=https://outreachpass.base2ml.com/api/v1

# AWS Cognito Authentication (REQUIRED)
# Get these values from AWS Console > Cognito > User Pools
NEXT_PUBLIC_COGNITO_USER_POOL_ID=us-east-1_XXXXXXXXX
NEXT_PUBLIC_COGNITO_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx
NEXT_PUBLIC_COGNITO_DOMAIN=your-cognito-domain

# AWS Region
NEXT_PUBLIC_AWS_REGION=us-east-1

# Application URL
NEXT_PUBLIC_APP_URL=https://outreachpassapp.base2ml.com

# Notes:
# - All NEXT_PUBLIC_* variables are exposed to the browser
# - Cognito credentials are REQUIRED for authentication to work
# - Never commit .env.local to version control
